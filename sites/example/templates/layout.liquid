<!DOCTYPE html>
<html lang="{{locale}}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{meta_title | default: site.name}}</title>
  <meta name="description" content="{{meta_description | default: site.description}}">
  <link rel="canonical" href="{{current_url}}">
  <script src="https://cdn.tailwindcss.com"></script>

  {% partial "_seo" %}
</head>
<body class="min-h-screen flex flex-col">
  <header class="bg-white shadow">
    <div class="max-w-6xl mx-auto px-4 py-4 flex items-center justify-between">
      <a href="/" class="text-xl font-bold">{{site.name}}</a>

      <!-- Desktop Navigation -->
      <nav class="hidden md:flex gap-6" aria-label="Main navigation">
        <a href="/services" class="hover:text-blue-600">Services</a>
        <a href="/about" class="hover:text-blue-600">About</a>
        <a href="/blog" class="hover:text-blue-600">Blog</a>
        <a href="/contact" class="hover:text-blue-600">Contact</a>
      </nav>

      <!-- Mobile Menu Button -->
      <button class="md:hidden p-2" onclick="document.getElementById('mobile-menu').classList.toggle('hidden')" aria-label="Toggle menu">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
        </svg>
      </button>
    </div>

    <!-- Mobile Navigation -->
    <nav id="mobile-menu" class="hidden md:hidden border-t" aria-label="Mobile navigation">
      <div class="px-4 py-2 space-y-2">
        <a href="/services" class="block py-2 hover:text-blue-600">Services</a>
        <a href="/about" class="block py-2 hover:text-blue-600">About</a>
        <a href="/blog" class="block py-2 hover:text-blue-600">Blog</a>
        <a href="/contact" class="block py-2 hover:text-blue-600">Contact</a>
        <a href="tel:{{site.phone}}" class="block py-2 text-blue-600">{{site.phone}}</a>
      </div>
    </nav>
  </header>

  <main class="flex-grow">
    {{body}}
  </main>

  <footer class="bg-gray-800 text-white py-12" itemscope itemtype="https://schema.org/LocalBusiness">
    <div class="max-w-6xl mx-auto px-4">
      <div class="grid md:grid-cols-3 gap-8">
        <div>
          <h3 class="font-bold text-lg mb-4" itemprop="name">{{site.name}}</h3>
          <div itemprop="address" itemscope itemtype="https://schema.org/PostalAddress">
            <p>
              <span itemprop="streetAddress">{{site.address}}</span><br>
              <span itemprop="addressLocality">{{site.city}}</span>,
              <span itemprop="addressRegion">{{site.region}}</span>
              <span itemprop="postalCode">{{site.postal_code}}</span><br>
              <span itemprop="addressCountry">{{site.country_code | upcase}}</span>
            </p>
          </div>
          <p class="mt-4">
            <a href="tel:{{site.phone}}" itemprop="telephone" class="hover:text-gray-300">{{site.phone}}</a>
          </p>
          <p>
            <a href="mailto:{{site.email}}" itemprop="email" class="hover:text-gray-300">{{site.email}}</a>
          </p>
        </div>

        <div>
          <h3 class="font-bold text-lg mb-4">Quick Links</h3>
          <nav aria-label="Footer navigation">
            <ul class="space-y-2">
              <li><a href="/services" class="hover:text-gray-300">Services</a></li>
              <li><a href="/about" class="hover:text-gray-300">About Us</a></li>
              <li><a href="/blog" class="hover:text-gray-300">Blog</a></li>
              <li><a href="/contact" class="hover:text-gray-300">Contact</a></li>
              <li><a href="/privacy" class="hover:text-gray-300">Privacy Policy</a></li>
            </ul>
          </nav>
        </div>

        <div>
          <h3 class="font-bold text-lg mb-4">Our Services</h3>
          <nav aria-label="Services navigation">
            <ul class="space-y-2">
              {% posts type:"category", limit:5, name:"footer_services", order:"alpha" %}
              {% for s in footer_services %}
              <li><a href="{{s.permalink}}" class="hover:text-gray-300">{{s.title}}</a></li>
              {% endfor %}
            </ul>
          </nav>
        </div>
      </div>

      <div class="border-t border-gray-700 mt-8 pt-8 flex flex-col md:flex-row justify-between items-center">
        <p class="text-gray-400 text-sm">
          &copy; {{year}} <span itemprop="name">{{site.name}}</span>. All rights reserved.
        </p>
        <p class="text-gray-400 text-sm mt-2 md:mt-0">
          Serving {{site.city}}, {{site.region}}
        </p>
      </div>
    </div>
  </footer>
</body>
</html>
